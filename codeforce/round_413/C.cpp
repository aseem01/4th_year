#include<bits/stdc++.h>
using namespace std;
//long long int x[200010],y[200010];
int main()
{
 int n,c,d;
 while(n>>c>>d)
 {
     /*int bakicoin,bakidimon;
     int pos=-1;
     char z,check;
     int value;
     int sum=-1;
     for(int i=0;i<n;i++)
     {
         cin>>x[i]>>y[i]>>z;
        // sum=-1;
         if(y[i]<=c || y[i]<=d)
         {
             //sum=max(sum,n);
             if(sum<n)
             {
                 sum=n;
                 x[pos]=value;
             if(z=='C') bakicoin=c-y[i];
             if(z=='D') bakidimon=d-y[i];
             x[i]=-1;
             pos=i;
             value=n;
             check=z;
             }
         }
     }
     if(check=='C') bakicoin=bakicoin;
     else bakicoin=c;
     if(check=='D') bakidimon=bakidimon;
     else bakidimon=d;
     //int paichi=sum;
     int sum1=-1;
     for(int i=0;i<n;i++)
     {
         if(((y[i]<=bakicoin) ||(y[i]<=bakidimon)) && x[i]!=-1)
         {
             //sum=max(sum,n);
             if(sum1<n)
             {
                 sum1=n;
                 //x[pos]=value;
             if(z=='C') bakicoin=bakicoin-y[i];
             if(z=='D') bakidimon=bakidimon-y[i];
             //x[i]=-1;
             //pos=i;
             //value=n;
             }
         }
     }
     if(sum!=-1 || sum1!=-1) cout<<sum+sum1<<endl;
     else cout<<"0"<<endl;*/

 }
}
